// src/components/SearchMovie.jsx — kept for backward compat, now a thin wrapper
// The main search is in Navbar. This component is no longer used directly.
import React from "react";

const SearchMovie = () => null;

export default SearchMovie;
